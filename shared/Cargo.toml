[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.40", features = ["serde"] }
chrono-humanize = { version = "0.2.3", features = ["wasmbind"] }
serde = "1.0.219"
