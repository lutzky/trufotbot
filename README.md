TODO: Document "release building" by building frontend, copying frontend/dist into server/src/assets, and then building server

TODO: Document "dev running" by running server with cargo watch and frontend with:

```
trunk serve --proxy-backend=http://localhost:3000/api
```
